<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:id="@android:id/background">
        <shape>
            <corners android:radius="5dip" />
            <gradient
                android:startColor="@color/backgunag"
                android:centerColor="@color/backgunag"
                android:centerY="0.75"
                android:endColor="@color/backgunag"
                android:angle="270"
                />
        </shape>
    </item>


    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <corners android:radius="5dip" />
                <gradient
                    android:startColor="@color/base_color"
                    android:centerColor="@color/base_color"
                    android:centerY="0.75"
                    android:endColor="@color/base_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>